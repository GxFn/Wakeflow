# 九片进度对应的图谱更新

> 状态：本轮更新与验证完成。
> 依据：用户 2026-09-11 提供的进度图与当前代码 `7ba1f38`；截图只用于确定核对范围，实际实现由代码和测试证明。

## 范围

更新旧 01–10 图集，补充 11 内核、12 端点、13 需求包、14 证据、15 Pod。保留旧目录深链。当前流程覆盖：需求包发布与看板、Pod 作用域 Demand、任务锚点及谱系、统一投递、固定回调、两类评审、测试合同与基线、完成/取消归档和继续、证据来源、Pod 创建与两段关闭。

observation 只画停止点；没有扩张到 L2 真实宿主、skills/commands 重写、L3/E4 制品切换，在线 FigJam 按用户追加授权同步三组摘要。

## 关键语义变化

| 原图表达 | 本次按代码更新 |
| --- | --- |
| 四静态注册组，23 工具 | 静态目录汇总切片登记，19 工具 |
| Confirmation/TODO 摄入链 | 两文档需求包，一次发布上板，认领即创建 |
| 任务 preview/apply | 幂等键和预期修订的一次追加 |
| 单独 TestCard、Test Delivery、Resume、缺陷授权工具 | testContract、统一投递、携 resumption 的决定和同提交缺陷授权 |
| indeterminate 永久停止 | 不自动重发；匹配结果可提交并释放精确声明 |
| 第一次 inspect 写回调确认 | inspect 只读，acknowledged 从已有决定派生 |
| 完成后归档未实现 | 终态、封归档、看板结算、取消声明释放、活动根退休 |
| Pod 作为远期停止点 | 配置、窗口与 worktree 回执共同形成当前 Pod 能力 |
| 跨 owner 总状态机 | 业务接力用流程/时序，各对象状态分别表达 |

## 保留的实际限制

配置仍 v3；Foundation 物理收敛和目录体积目标未全部完成。Pod ready 是派生视图，create_demand 当前检查存在、open 与空闲而不以 ready 作为独立机器门。调用者角色主要由 skills 约定；截图的“九片完成”不等于所有宿主与最终产品验收已经完成。

## 证据与验证

[代码基线验证](./evidence/l1-nine-slices-facts.json) 记录 851 项根测试、16 个场景、93 Schema、19 工具及当前目录体积。图谱检查、渲染回执和浏览器阅读结果见下表。

上轮 44 图的旧编号和来源保存在历史审查快照中；本轮按现存生产者、消费者和符号重新形成关系，并使用独立的 E-L1 数字编号。没有只更新摘要来使过期图通过。


## 收尾时的并行来源修改

核验过程中另一个修改将 `src/kernel/command-shell.ts` 的注释从“admit 在打开根之前”更正为“admit 在打开上下文之前”。本窗口核对了实际 diff 和函数顺序；45 份来源指纹包含该差异，因此本次提交一并携带此注释修正，保证重新检出时可以复验。没有运行时语句、Schema 或测试变化。本轮内核图原本就是先固定根、再进入上下文，没有依照旧注释画错顺序。受影响的来源指纹在复核后重新计算，851 项根测试仍引用已验证的 `7ba1f38` 代码基线，不冒充重新执行过的一轮测试。


## 本轮交付与验证

| 项目 | 结果 |
| --- | --- |
| 图谱覆盖 | 48 份 maps 文档，67 张 Mermaid 图；15 个专题 |
| 来源核验 | 46 份指纹当前，0 漂移；164 条 AST 直接导入、268 处符号引用、454 行邻接边证据检查通过 |
| 根代码门 | `7ba1f38` 基线的 npm test 通过：851 测试、16 场景、93 Schema；收尾并行改动仅一行注释，已另行核对 |
| 图谱门 | npm run check 通过：8 项检查器/阅读器回归、类型、严格来源门、67 图浏览器实测回执、构建 |
| 实际渲染 | 应用内浏览器按锁定 Mermaid 11.17.2 渲染 67/67，保留逐图源码摘要及非零 SVG 尺寸 |
| 阅读检查 | 暗/浅色主题、四状态图、依赖图全屏、完整文件路径、节点到证据定位、下钻链接、切页滚动和控制台统计均已检查 |
| 构建提示 | 保留原有大型 chunk 警告；没有放宽预算来隐藏提示 |
| FigJam 同步 | 三组摘要、29 个可编辑节点、32 条边；附进度表，旧图折叠保存；结构与截图检查通过 |
| 未执行 | 真实宿主投递、L2 联合、新插件发布/切换；这些仍不在截图所示已完成阶段内 |

[根验证与工具/差异事实](./evidence/l1-nine-slices-facts.json) · [实际浏览器渲染回执](./evidence/l1-nine-slices-render.json) · [图谱检查摘要](./evidence/l1-nine-slices-validation.json) · [FigJam 同步记录](./evidence/l1-nine-slices-figjam.json)

命令行无头浏览器在当前宿主超时，因此本轮用应用内浏览器执行实际渲染。`check:diagrams` 复验该真实回执与当前每张图的源码、Mermaid 版本、数量及成功尺寸；改图后必须重新打开本地验证页生成回执，不会把旧回执当作新渲染。此回执证明语法与可渲染性，人工阅读检查仍单独记录。
